export const login=(username, password)=>{
    return {
        type:"LOGIN",
        payload:{
            username:username,
            password:password
        }
    }
}

export const loginAction = (username, password) =>{
    return (dispatch)=>{
        
    }
}